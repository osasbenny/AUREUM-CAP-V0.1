# CAP-001 Analytics

Analytics status: **CLIENT_EVENT_CONTRACT_IMPLEMENTED**.

The website records privacy-scoped events in `window.__CAP_EVENTS__` with `prospect_id`, `demo_id`, `campaign_id`, timestamp, and scoped session ID. No production analytics backend is claimed as deployed or verified.
