import './style.css';
import leads from '../data/leads.json';

const state={query:'',category:'All',stage:'All',selected:new Set(),reviewed:false};
const categories=['All',...new Set(leads.map(l=>l.category))];
const readiness=[['DATABASE','PARTIALLY READY','Persistence adapter pending'],['STORAGE','READY','Import artifact packaged'],['QUEUE','PARTIALLY READY','Provider wiring pending'],['SCHEDULING','PARTIALLY READY','Campaign schedule defined'],['EMAIL','BLOCKED','SES identity + sandbox review required'],['SECRETS','BLOCKED','Configure managed secret store'],['MONITORING','PARTIALLY READY','Baseline events documented'],['HUNTER','BLOCKED','API credential not configured'],['OPENAI','BLOCKED','API credential not configured']];
function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function render(){
 const filtered=leads.filter(l=>(state.category==='All'||l.category===state.category)&&(state.stage==='All'||l.stage===state.stage)&&(`${l.name} ${l.location} ${l.category}`.toLowerCase().includes(state.query.toLowerCase())));
 const selectedCount=state.selected.size;
 document.querySelector('#app').innerHTML=`<div class="shell"><aside><div class="brand"><div class="mark">A</div><div><strong>AUREUM</strong><span>CAP / V0.1</span></div></div><nav><a class="active">Overview</a><a>Leads <b>100</b></a><a>Campaigns</a><a>Revenue</a></nav><div class="side-note"><small>PILOT STATUS</small><strong>Review before send</strong><p>No outreach is sent from this console. Every lead must be verified, enriched, approved, and released in controlled batches.</p></div></aside><main><header><div><div class="eyebrow">CAP V0.1 · CAMPAIGN 001</div><h1>Houston Website Opportunity</h1><p class="sub">A controlled acquisition pipeline for the first 100 supplied prospects.</p></div><div class="header-actions"><span class="pill amber">● Smoke test</span><button class="ghost" id="export">Export CSV</button></div></header><section class="hero"><div><span class="eyebrow">OPERATIONS BRIEF</span><h2>Quality first. <em>Replies</em> next.</h2><p>The pilot is prepared to move prospects through verification, scoring, product matching, and human review. Sending remains intentionally gated.</p></div><div class="hero-stat"><strong>${leads.length}</strong><span>imported prospects</span><div class="progress"><i style="width:100%"></i></div><small>Dataset integrity check passed</small></div></section><section class="metrics"><div class="metric"><span>Imported</span><strong>100</strong><small>from supplied PDF</small></div><div class="metric"><span>Website status</span><strong>0<span class="slash"> / </span>100</strong><small>verified / pending</small></div><div class="metric"><span>Review batch</span><strong>0<span class="slash"> / </span>20</strong><small>approved / target</small></div><div class="metric"><span>Pipeline value</span><strong>$0</strong><small>no opportunities yet</small></div></section><section class="grid"><div class="panel leads-panel"><div class="panel-head"><div><span class="eyebrow">PROSPECT QUEUE</span><h3>Lead review queue</h3></div><span class="count">${filtered.length} shown</span></div><div class="toolbar"><input id="search" placeholder="Search businesses, locations..." value="${esc(state.query)}"/><select id="category">${categories.map(c=>`<option ${c===state.category?'selected':''}>${esc(c)}</option>`).join('')}</select><select id="stage"><option>All</option><option>IMPORTED</option><option>VERIFIED</option><option>APPROVED</option></select></div><div class="batchbar"><label><input type="checkbox" id="selectAll"> Select visible</label><span>${selectedCount} selected</span><button class="primary" id="review">Mark for review</button></div><div class="table-wrap"><table><thead><tr><th></th><th>Business</th><th>Category</th><th>Location</th><th>Website</th><th>Stage</th></tr></thead><tbody>${filtered.slice(0,30).map(l=>`<tr><td><input class="lead-check" data-id="${l.id}" type="checkbox" ${state.selected.has(l.id)?'checked':''}></td><td><strong>${esc(l.name)}</strong><small>${esc(l.phone)}</small></td><td><span class="tag">${esc(l.category)}</span></td><td>${esc(l.location.replace('LOCATION: ','').replace(' PHONE NUMBER:',''))}</td><td><span class="status neutral">Unverified</span></td><td><span class="stage">${l.stage}</span></td></tr>`).join('')}</tbody></table></div><div class="table-foot">Showing first 30 of ${filtered.length}. Website and email status are unverified by design.</div></div><div class="right-stack"><div class="panel readiness"><div class="panel-head"><div><span class="eyebrow">CONTROL PLANE</span><h3>Production readiness</h3></div></div>${readiness.map(r=>`<div class="ready-row"><span>${r[0]}</span><div><b class="${r[1].toLowerCase().replace(' ','-')}">${r[1]}</b><small>${r[2]}</small></div></div>`).join('')}</div><div class="panel next"><span class="eyebrow">NEXT ACTION</span><h3>Verify before enrich</h3><p>Run website verification and deduplication before Hunter or AI calls. Start with 10–20 high-fit prospects after human review.</p><button class="outline" id="docs">View runbook →</button></div></div></section><footer><span>© Aureum Cloud · CAP V0.1</span><span>Founder operator: Osagie Bernard Ebhuomhan</span><span class="safe">● Sending disabled</span></footer></main></div>`;
 bind(filtered);
}
function bind(filtered){document.querySelector('#search').oninput=e=>{state.query=e.target.value;render()};document.querySelector('#category').onchange=e=>{state.category=e.target.value;render()};document.querySelector('#stage').onchange=e=>{state.stage=e.target.value;render()};document.querySelectorAll('.lead-check').forEach(x=>x.onchange=()=>{const id=+x.dataset.id;x.checked?state.selected.add(id):state.selected.delete(id);render()});document.querySelector('#selectAll').onchange=e=>{filtered.slice(0,30).forEach(l=>e.target.checked?state.selected.add(l.id):state.selected.delete(l.id));render()};document.querySelector('#review').onclick=()=>{state.reviewed=true;alert(state.selected.size?`${state.selected.size} prospect(s) marked for human review. No messages were sent.`:'Select prospects first.');};document.querySelector('#export').onclick=()=>{const csv=['id,name,category,location,phone,websiteStatus,stage',...leads.map(l=>[l.id,l.name,l.category,l.location,l.phone,l.websiteStatus,l.stage].map(v=>`"${String(v).replaceAll('"','""')}"`).join(','))].join('\n');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv'}));a.download='aureum-cap-campaign-001-leads.csv';a.click()};document.querySelector('#docs').onclick=()=>alert('Runbook: import → deduplicate → verify website → enrich → verify email → score → match product → generate message → human approve → queue.');}
render();
