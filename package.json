{
  "name": "aureum-cap-v0-1",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "api": "node server/index.mjs",
    "check": "node --check server/index.mjs",
    "validate:pilot": "node scripts/validate-pilot.mjs",
    "smoke": "npm run check && npm run validate:pilot && npm run build"
  },
  "devDependencies": {"vite":"^7.1.7"}
}
